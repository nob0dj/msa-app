# app-config-server
server:
 port: 8888

spring:
  application:
    name: app-config-server
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/nob0dj/msa-configs.git'
          